# Auth0 Configuration
# Get these values from your Auth0 dashboard
# https://manage.auth0.com/dashboard/us/YOUR_TENANT/applications

VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your_client_id_here
VITE_AUTH0_AUDIENCE=https://your-api-identifier (optional)

# For local development, these URLs should be:
# - Callback URL: http://pcs.localhost:3000
# - Logout URL: http://pcs.localhost:3000
# - Allowed Web Origins: http://pcs.localhost:3000

# For production, Netlify will set these automatically via the Auth0 extension

# LiveStore Sync Configuration (Optional)
# If set, enables real-time sync with a backend server
# Leave unset for local-only development (data persists in browser OPFS)
# VITE_LIVESTORE_SYNC_URL=http://localhost:8787
